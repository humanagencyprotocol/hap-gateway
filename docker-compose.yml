services:
  hap:
    build: .
    ports:
      - "${HAP_UI_PORT:-7000}:3000"
      - "${HAP_MCP_PORT:-7030}:3030"
    volumes:
      - ${HOME}/.hap:/app/data
    environment:
      - HAP_SP_URL=https://www.humanagencyprotocol.com
    network_mode: bridge
    restart: unless-stopped
